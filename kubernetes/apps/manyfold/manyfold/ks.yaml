apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: manyfold
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./kubernetes/apps/manyfold/manyfold/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: flux-system
    # Provides the shared bjw-s HelmRepository.
    - name: helm-repositories
    - name: external-secrets-config
    - name: longhorn
    - name: csi-driver-smb-config
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: manyfold
      namespace: default
