apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grimmory
  namespace: default
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: grimmory
      port:
        number: 6060
  tls:
    - hosts:
        - grimmory
